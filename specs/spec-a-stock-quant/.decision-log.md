# Decision Log — SPEC-a-stock-quant

This log is the canonical memory for the spec run. Downstream skills do not read it; it exists for the spec author and audit.

## Inputs

- **Pre-supplied:** `/Users/wangdecheng/ai/stock/wayfinder/MAP.md` (the wayfinder map)
- **Implied sources (read):** all 12 tickets in `wayfinder/tickets/` (T1, T2, T3, T4, T5, T6, T7, T8, T9, T10, T11, T12)
- **Handoff context:** `/var/folders/.../a-stock-handoff.ytsMrXTLvR.md` (private note from prior session — included the user preference about detailed explanations and constraints known to the effort)

## Slug

`a-stock-quant` — derived from the wayfinder's destination ("A 股量化 Web 应用 MVP"). The handoff doc called it "A 股量化 Web 框架".

## Mode

Headless — invoked from the `/to-spec` slash command with a fully-formed intent (9 decisions + 7 fog items), no further elicitation needed. The handoff already provided scope and constraints; the session is programmatic, not interactive.

## Workspace decision

The handoff said "SPEC.md 或类似路径". I placed the spec under `/Users/wangdecheng/ai/stock/specs/spec-a-stock-quant/` (skill convention: `{output_folder}/specs/spec-{slug}/`) because:

1. The folder layout supports companions cleanly (SPEC.md + 8 sibling companions + decision log).
2. `/to-tickets` downstream skills expect a folder that follows this convention; placing the SPEC directly at the project root would have starved `/to-tickets` of companions.

The handoff's "类似路径" wording permits this.

## Operation choice

Chose **express** (Spec Law §"The Operation"). The input is rich and pre-sorted into 9 decisions + 3 out-of-scope tickets + 7 fog items; elicitation would have re-litigated what's already in `Resolution` blocks.

## Capsule — distillation summary

| # | Wayfinder decision | Lands in SPEC as |
|---|---|---|
| D1 | AKShare single source + DataAdapter + ratelimit + cache fallback | CAP-1 + `data-adapter.md` + Constraint 1/2/3 |
| D2 | Self-written engine ~200 lines + empyrical | CAP-2 + `backtest-engine.md` + Constraint 7/8 |
| D3 | target_position + Context dataclass + auto-scan + __init__ form | CAP-3, CAP-4 + `strategy-interface.md` + Constraints 4/5/6 |
| D4 | pandas-ta wrapper (separate from empyrical) | Constraint 12 + `indicators-and-metrics.md` |
| D5 | Streamlit single-process + 5 pages multipage + off-process scheduler | CAP-6/7/8 + `ui-pages.md` + `scheduler.md` + Constraint 8/13/14/15 |
| D6 | Dual-track SQLite (real + per-strategy virtual) | CAP-5 + `position-schema.md` + Constraint 9/10/11 |
| D7 | 5-page desktop-first + reverse-proxy basic auth | CAP-6 + `ui-pages.md` + Constraint 14/15 |
| D8 | Pyecharts + streamlit-echarts[pyecharts] | Constraint 12 + `ui-pages.md` snippets |
| D9 | 15:30 scheduler outside Streamlit process | CAP-7 + `scheduler.md` + Constraint 13 |

## Pass 1 — Coherence

Each capability has intent + success: 8/8. Intents describe WHAT (the "interface with rate-limit guarantees" phrasing names WHAT qualities without prescribing implementations that aren't already in companions). All 15 constraints are decision-benders (verified one by one in the self-validate scratch). 9 non-goals are explicit. Success signal names concrete testable artifacts (5-page UI, PnL curve + 6 metric cards, 1% tolerance, 5-minute freshness, basic-auth-gated browser). Capability IDs CAP-1 through CAP-8 are unique, monotonic, and reserved for future revision (no reuse).

Two follow-ups caught:

1. **"rounded to 100 shares (A-share board lot)"** in `backtest-engine.md` — wrong for ETFs. Fixed to differentiate stock vs ETF vs fund lots.
2. **CAP-4 success criteria** had "(rounding to lots)" which read as A-share lot-only. Replaced with "drift bounded by share-rounding and next-day-open fill price" — factually accurate across instrument classes.

Assumptions list covers 6 calls (AKShare maintenance trajectory, single-user / CN-only, desktop Chrome primary, server topology, Chinese UI, no GUI strategy-author tool). Each is justified by either explicit user framing or the handoff doc.

Open Questions mirrors the fog block in `wayfinder/MAP.md` 1:1 plus one added (reverse-proxy basic-auth credential rotation, which is operationally relevant even if out of effort).

## Pass 2 — Preservation

Walked each ticket's Resolution block claim-by-claim against SPEC.md + companions. All claim lines from T1, T2, T3, T5, T6, T10, T11, T12 land in at least one artifact. T4's research findings (`wayfinder/research/akshare-coverage.md`) is referenced through `data-adapter.md`'s "AKShare interface mapping" and "Revive condition" columns but not duplicated — the research is the canonical reference, the companion only quotes the decision-relevant slice (capability table + known gaps + recovery path).

Wrapper-only drops (process metadata, ticket claim history, ticket-type metadata): none preserved — these are not load-bearing claims, per Spec Law rule 8.

## Verdict

The spec satisfies Spec Law on all eight rules after the two corrections above. Capability IDs are stable for downstream. Open Questions track the handoff's fog 1:1 with one added item (basic-auth credential rotation). All load-bearing decisions from the 9 resolution blocks land in either the kernel or a companion. No content was invented beyond what the source supported (the few Assumptions entries are inferences clearly marked as such and rooted in the handoff / destination prose).
