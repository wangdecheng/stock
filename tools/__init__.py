"""tools — CLI utilities shipped with the project.

Currently houses the multi-universe batch backtest runner
(``run_batch_backtest.py``) introduced for the ADX+BB regime strategy.
Future utilities (e.g. the baseline comparison tool from
``.scratch/adx-bb-regime/spec.md`` D1) live alongside it.
"""
