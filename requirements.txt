# Runtime
streamlit>=1.30,<1.49  # <1.49 because streamlit-echarts 0.4.x predates Streamlit's v2 component asset_dir enforcement (1.50+). Re-evaluate if we upgrade streamlit-echarts.
pyecharts==2.0.9
streamlit-echarts[pyecharts]>=0.4.0,<0.5  # 0.5+ switched to v2 components, which require Streamlit 1.50+ AND asset_dir in pyproject.toml (which 0.x doesn't declare).
akshare>=1.16.0
pandas>=2.0,<3.0
pyarrow>=14.0
empyrical>=0.5.5      # T3: portfolio metrics (Sharpe / max drawdown / Calmar)
# pandas-ta — T12: 0.4.67b0+ requires Python 3.12; defer until base Python is 3.12+
# Placeholder so the seam is documented; uncomment when base Python moves up.

# Dev / test
pytest>=8.0